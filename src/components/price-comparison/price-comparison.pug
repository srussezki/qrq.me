.price-comparison.container
    h5.title Preisvergleich
    
    .comparison-box.container
    each offer, i in offers
        .row.comp-row
            .col-3
                img.comp-logo(src=offer['shop-logo']) 
            .col-3
                span= (offer['offer-price']*1).toFixed(2).replace('.',',') + " €"
            .col-6
                a.offer-link(href=offer['offer-link'] target='_blank')= offer['shop-name']
